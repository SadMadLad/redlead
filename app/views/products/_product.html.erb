<div class="flex flex-col gap-1 border border-blue-500 rounded-lg p-4" id="<%= dom_id product %>">
  <p>Product Title: <%= product.title %></p>
  <p>Product Description: <%= product.description %></p>
  <%= button_to "Delete", business_product_path(@business, product), method: :delete, class: "text-white text-lg font-bold rounded-lg bg-red-500 px-2.5 py-1.5 cursor-pointer" %>
</div>
